你是一个资深全栈架构师 + 产品工程师 + LLM 应用设计师。  
请你为我设计并实现一个 MVP：基于自然语言生成现实地图上的旅行计划可视化。

【项目目标】
用户输入一段自然语言，例如：
“我想去京都玩三天，想去清水寺、伏见稻荷、岚山，住在地铁方便、市中心的位置，希望行程别太赶。”

系统需要自动完成：
1. 解析用户输入中的城市、景点、旅行偏好、天数、约束条件；
2. 在现实地图上标出景点、住宿区域、地铁站等关键点位；
3. 自动规划路线顺序，并在地图上画出路线；
4. 输出一份结构化的旅行计划，方便前端直接渲染；
5. 仅做核心 MVP，不要加入登录、注册、收藏、社交、推荐流、支付等无关功能；
6. 数据只做本地化存储，不接云端账号体系。

【技术选型要求】
请你自行选择最适合快速开发的技术栈，优先考虑开发效率、可维护性、易于接入地图与大模型能力。
推荐优先方案：
- 前端：React + Vite
- 后端：Node.js + Express
- 地图：Google Maps JavaScript API
- 大模型：DeepSeek API
- 本地存储：IndexedDB（前端）+ 可选 SQLite（后端）

如果你认为更适合用 Vue 或 Python，也可以说明理由并给出替代方案，但默认请按“开发效率优先”的方案设计。

【核心能力边界】
1. 系统只负责“旅行计划地图”；
2. 不要写普通旅游攻略，不要输出长篇散文；
3. 不要编造经纬度，如果没有可靠坐标就填 null；
4. 不要假设不存在的数据源；
5. 不要实现登录、个人中心、云同步、收藏夹、评论等功能；
6. 不要让大模型直接操作 UI，应该采用“LLM 生成结构化指令 → 前端执行渲染”的架构；
7. 重点是把自然语言变成可渲染的地图数据，而不是聊天。

【推荐架构】
请你按以下思路设计：
- LLM 负责：理解用户意图、生成 JSON 结构化规划结果、生成路线排序、生成解释文本；
- 后端负责：调用地图服务、补齐地点坐标、整理数据；
- 前端负责：展示地图、打点、画线、显示结果卡片；
- 本地存储负责：保存历史规划记录、最近使用的城市、用户偏好模板。

【需要实现的功能】
MVP 必须包含：
1. 输入框：用户输入自然语言需求；
2. 解析结果：识别城市、景点、偏好、天数、约束；
3. 地图展示：标记景点、住宿区域、可选地铁站；
4. 路线绘制：把景点按顺序连成路线；
5. 推荐结果：给出最适合的住宿区域和路线理由；
6. 本地保存：保存最近的旅行规划结果，供用户下次打开查看；
7. 重新生成：用户修改偏好后可重新规划；
8. 基础容错：信息不足时给出最少必要的澄清问题。

【输出要求】
请你输出以下内容，按顺序给我：

1. 项目总体方案
   - 产品定位
   - MVP 范围
   - 用户流程
   - 关键技术点

2. 系统架构设计
   - 前端、后端、LLM、地图服务、本地存储之间如何协作
   - 数据流图的文字版说明
   - 为什么这样设计最稳

3. 数据结构设计
   - 旅行请求结构
   - 旅行计划结构
   - 地图标记结构
   - 路线结构
   - 本地存储结构

4. 大模型提示词设计
   - System Prompt
   - User Prompt 模板
   - JSON 输出 schema
   - 信息不足时的澄清策略
   - 如何减少模型胡编乱造

5. 前后端接口设计
   - 请求与响应格式
   - 接口命名
   - 错误处理
   - 地点坐标补齐逻辑

6. 前端页面设计
   - 页面布局
   - 地图区域
   - 结果面板
   - 输入区
   - 计划展示区

7. 代码工程结构
   - 目录结构
   - 每个文件的职责
   - 哪些部分先写，哪些后写

8. MVP 开发步骤
   - 按优先级分阶段实施
   - 每阶段的验收标准
   - 哪些功能必须先做，哪些可以延后

9. 给出最小可运行版本的代码骨架
   - 前端主页面
   - 后端 API
   - LLM 调用封装
   - 地图渲染核心
   - 本地存储封装

【对输出风格的要求】
- 先给结论，再展开；
- 语言要直接、工程化、可执行；
- 不要空泛；
- 不要只讲概念，必须落到实现；
- 如果有不确定的地方，要明确说明假设；
- 如果方案有多个选项，请直接给推荐方案，不要把我淹没在选项里。

【特别强调】
这不是一个完整旅行平台，而是一个极简但可用的 MVP。  
请围绕“自然语言生成现实地图旅行计划”这个单点需求来设计，不要扩展到其他商业功能。

请开始输出完整方案。