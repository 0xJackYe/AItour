# 更新日志

本文件根据项目源码、文件时间以及 Codex 中该项目的历史开发记录整理。项目在本次上传前没有 Git 历史，因此以下内容按实际开发阶段还原，而不是从旧提交记录生成。

## 2026-08-01 - 连续可执行行程与深度偏好重构

### 旅行资料与生成可靠性

- 输入区新增结构化旅行资料：天数、日期、预算金额与口径、节奏、交通优先级、允许/避免方式、同行人、步行上限、作息、住宿、兴趣、饮食及无障碍需求。
- 预算新增可选硬上限；费用改为包含金额、币种、群组/每人口径和置信度的结构，不再把 `null`、空值、异币种或缺少口径的费用当成 0 元计入覆盖率。
- 四项核心资料缺失时返回可交互的结构化问题；短行程和长行程都会把完整 profile 作为硬约束注入模型。
- 生成期间保留当前可用方案，加入请求取消和请求 ID 防护，避免失败清空方案或迟到请求覆盖历史版本。

### 可执行数据模型

- 新增稳定 Trip、Stage、Day、Node、Leg 和 Connection ID。
- 每天自动加入住宿起终点、逐段交通、出发/到达和活动时间；相邻日期通过住宿过夜或跨城连接串联。
- 同城更换酒店会按 `check_in_day` / `check_out_day` 选择当天住宿锚点并拆分阶段。
- 跨城日加入次日出发、抵达和 45 分钟行李/进城缓冲，再开始当天活动。
- 新增预算摘要及天数、ID、锚点、逐段连续性、路线状态、跨日、结束时间和预算覆盖的确定性健康检查。

### 路线与地图

- Google Routes 从整日固定步行改为逐段 WALK、TRANSIT、DRIVE、BICYCLE。
- 路线失败返回 `unavailable` 和 `geometry: null`，彻底移除把坐标直连后冒充 Google 步行路线的旧行为。
- 路线失败仍保留明确标注的编译估时，防止时间轴压缩成 0 分钟；只在用户允许且不违反单段步行上限时回退交通方式。
- RAIL、FLIGHT 使用 `needs_confirmation` 和明确估时，不伪造地图线路。
- 地图与时间轴双向选择，支持单日和全程概览；全程仅展示住宿、城市阶段和真实跨城线路。
- 地理编码与交通点查询加入受控并发及多城市住宿覆盖；错误地点保留为待修复节点，不再静默消失。

### 界面、编辑与历史

- 新增旅程主轴、城市分组日期导航、Node/Leg 时间轴、跨日连接卡和统一计划健康抽屉。
- 支持普通活动锁定、上下移动、删除、服务端重新计算、撤销和重做；住宿与起终点锚点受到保护。
- IndexedDB 升级为完整版本快照，同时兼容旧历史记录。
- 桌面端升级为 44%/56% 计划地图布局；移动端改为 `100dvh` 地图与底部行程面板，并补齐键盘、ARIA 和触控状态。

### 安全与质量门禁

- 拆分浏览器 Maps JavaScript Key 和服务端 Places/Routes Key 配置。
- 前端构建彻底移除服务端 Key 回退；未配置浏览器 Key 时安全降级。服务端新增生产来源白名单、请求体限制和昂贵规划接口的 IP 限流。
- AI 内容复核缺密钥、超时或解析失败时返回 `unknown`，不再伪称审核通过。
- 升级 Vite、Express 及传递依赖，根项目、服务端和客户端依赖审计均清零。
- 新增前端数据层测试、服务端纯函数与真实 HTTP 契约测试、统一 `npm run check` 和 GitHub Actions 质量门禁。
- 发布前真实浏览器覆盖桌面与 390×844 移动视口、真实 AI 生成、交通回退/失败语义、健康详情、移动重算、撤销/重做和历史抽屉。

## 2026-07-16 - GitHub 首次发布准备

### 新增

- 新增项目说明 `README.md`，记录架构、功能、运行方法、验证命令、目录结构和已知边界。
- 新增 `.env.example`，列出 DeepSeek、Google Maps Platform 和服务端端口配置。
- 新增完整更新日志 `CHANGELOG.md`。
- 新增 `.gitignore`，排除密钥、依赖目录、构建产物、日志和本地工具配置。

### 安全

- 明确排除根目录 `.env` 和遗留密钥文件 `api.txt`，避免真实 DeepSeek/Google API Key 被公开提交。
- 文档补充生产环境浏览器 Key 与服务端 Key 分离、域名/IP/API 范围限制建议。

## 2026-07-15 - 长行程可靠性与地点消歧

### 长行程生成

- 将 12 天及以上请求识别为长行程，改用两阶段生成：先生成覆盖所有天数的紧凑骨架，再以每 7 天为一段补充详细内容。
- 将长行程分段确定性合并，强制校验 Day 1 到目标天数连续且完整；缺少任意一天时不再把结果当作成功计划。
- 增加多目的地结构：`destinations`、`accommodations`，以及每天和每个景点独立的 `city`、`country` 上下文。
- 支持一日跨城景点使用自己的城市锚点，不再错误绑定到当天住宿城市。
- 默认输出上限从原来的 4096 提升到 8192 tokens。

### LLM 输出容错

- 检测空内容、非法 JSON 和 `finish_reason: length`，对可恢复的输出问题自动重试。
- 支持从 Markdown 代码围栏和附带说明的模型回复中提取 JSON。
- 保留原有“生成 -> 自审 -> 严重问题最多重生成一次 -> 再次自审”流程，避免无限重试。

### 地点准确性

- 将 Google Places 的软性位置偏好升级为 `locationRestriction` 硬范围限制。
- 新增国家代码、城市视口、行政区组件和地点官方名称的多层校验。
- 针对东京都视口包含远海岛屿、范围异常宽的情况，增加 `Tokyo`/`Shizuoka` 等行政区组件对比，防止静冈同名景点混入东京计划。
- 单城市计划不能通过模型自行修改 `spot.city` 绕过目标城市约束。
- 地点名称与 Google 候选不匹配时直接剔除，并把原因加入地理编码警告与计划校验结果。
- 坐标键从景点名称改为“天数 + 景点序号”，同名景点不再互相覆盖。
- 地理编码后的被拒绝景点不会进入最终计划、地图标记或路线。

### 性能

- 每日 Google Routes 请求使用受控并发，降低长行程路线生成总耗时。

### 测试与验证

- 新增 11 项自动化回归测试，覆盖：
  - 28 天复杂请求识别和分段合并；
  - Day 1–28 完整性；
  - JSON 代码围栏提取；
  - 长度截断自动重试；
  - 单城市约束不可绕过；
  - 东京候选通过、静冈同名候选拒绝；
  - 国家不一致拒绝；
  - 地点名称匹配与英文常见词形差异；
  - 同名景点唯一键不冲突。
- 自动化测试结果：11/11 通过。
- 使用“塞尔维亚 -> 波黑 -> 克罗地亚 -> 意大利 -> 瑞士 -> 法国，28 天”的真实复杂请求完成端到端验证：
  - 完整返回 Day 1–28；
  - 塞尔维亚开始、法国结束；
  - 71 个最终景点；
  - 75 个地点成功地理编码；
  - 8 个名称不匹配的 Google 候选被拦截；
  - 未再出现“LLM 返回了无法解析的内容”。
- 使用东京真实浏览器场景验证浅间神社和浅草寺均正确定位，静冈候选未混入，页面与控制台无错误或警告。

## 2026-07-15 - Google 地图缩放性能优化

### 修复

- 定位缩放卡顿的三个主要来源：栅格瓦片逐级加载、高精度路线折线重投影、自定义 DOM 标记重排。
- 修复高级标记使用无效 Map ID 时触发 Google 地图加载错误的问题，使用兼容的 `DEMO_MAP_ID` 配置。
- 兼容开发态热更新前后不同类型的地图监听器，避免 HMR 清理阶段崩溃。

### 优化

- 支持硬件 WebGL 时优先启用 Google 矢量地图和 fractional zoom，实现连续缩放。
- WebGL 不可用或只能软件渲染时自动回退到栅格兼容模式，避免无条件强制矢量模式造成地图加载错误。
- Google Routes 折线质量改为 `OVERVIEW`：实测京都路线从约 1032 个点降到约 240 个点。
- 对旧历史记录中的路线在前端最多绘制 320 个点，避免高密度折线拖慢缩放。
- 景点标记替换为 Google 原生 `PinElement`，减少缩放期间 DOM 样式计算和重排。
- 页面显示“Google Maps · 矢量加速”或“兼容模式”，便于确认实际渲染路径。

### 验证

- 连续放大两级，从约 14.49 平滑缩放到 16.49。
- 路线、标记和信息窗口保持正常，控制台无警告和错误。
- 前端生产构建通过。

## 2026-07-14 - Google Maps Platform 全链路迁移

### 地图与数据源

- 前端地图从 Leaflet + OpenStreetMap 迁移到 Google Maps JavaScript API。
- 景点和住宿定位从 Nominatim 迁移到 Google Places Text Search 与 Geocoding。
- 住宿附近交通点从关键词拼接地理编码迁移到 Google Places Nearby Search。
- 每日道路路线从 OSRM 迁移到 Google Routes API。
- 移除 Leaflet、React Leaflet、OpenStreetMap、Nominatim 和 OSRM 的代码与依赖。

### 前端

- 新增 Google Maps 动态库加载服务。
- 地图支持 Google 原生地图/卫星切换、街景、全屏、缩放和信息窗口。
- 景点、住宿和交通点使用不同标记；地点详情支持跳转到 Google 地图。
- 保留 LLM 给出的景点顺序，由 Google Routes 返回真实道路折线。
- 保留 IndexedDB 本地行程历史。

### 后端

- 新增统一的 Google JSON 请求封装。
- 新增 Places 文本搜索、地址地理编码、附近交通点搜索与坐标转换。
- Routes API 返回步行模式、距离、时间和解码后的路线坐标。
- Google Key 与 DeepSeek Key 分离：DeepSeek 继续负责 AI 文案和结构化计划，Google 只负责地图与地理数据。

### 修复与验证

- 修复开发态热更新后旧版地图监听器不能按新方式清理的问题。
- 京都真实端到端行程验证通过：地点全部定位，附近交通站查询成功，Google Routes 返回约 7.9 km、1 小时 51 分的步行路线和道路折线。
- 浏览器确认 Google 标识、原生控件、地图/卫星、街景、全屏、缩放和外链均正常。
- 浏览器控制台无错误或警告；服务端语法检查、健康接口和前端生产构建通过。
- 迁移后前端生产 JS 产物由约 316 KB 降至约 164 KB。

## 2026-07-13 - Google API 接入验证

### 配置

- 保留原有 `DEEPSEEK_API_KEY`，新增独立的 `GOOGLE_MAPS_API_KEY`，避免错误地用 Google Key 替换大模型 Key。
- 根目录环境变量由服务端和 Vite 共同读取，供本地开发使用。

### 验证

- Geocoding API：成功返回京都站规范地址和坐标。
- Places API：成功检索清水寺及其坐标。
- Routes API：成功生成步行路线、距离和路线折线。
- Maps JavaScript API：脚本加载成功，无 Key、计费或权限错误。

## 2026-04-04 至 2026-04-27 - 初始 MVP

### 前端

- 使用 React + Vite 建立单页旅行规划界面。
- 提供自然语言输入、示例需求、解析结果、计划卡片、校验结果、附近交通、地图和历史记录面板。
- 使用 IndexedDB 保存最近的本地行程历史。
- 初始地图技术栈为 Leaflet + OpenStreetMap。

### 后端

- 使用 Node.js + Express 提供 `/api/plan` 和 `/api/health`。
- 接入 DeepSeek Chat Completions API，把自然语言需求转换为 JSON 旅行计划。
- 增加 LLM 自审：检查需求覆盖度和地点合理性，严重问题最多重生成一次。
- 初始地理编码使用 Nominatim，路线使用 OSRM，附近交通采用关键词地理编码。
- 输入为空时返回 400；生成、校验、地理编码、交通和路线由统一计划路由编排。

### 工程

- 提供根目录、前端和后端独立的 `package.json` 与锁文件。
- 提供 `start.bat` 和 `npm run dev`，同时启动 3001 端口后端与 5173 端口前端。
- 初始验证包括前端生产构建、服务端语法检查、健康接口和非法空请求。
